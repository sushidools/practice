/**
 * Merge two enums into one
 * The derived will overwrite duplicate keys from base
 *
 * @export
 * @template T
 * @template R
 * @param {T} base
 * @param {R} derived
 * @returns {(T & R)}
 */
export declare function mergeEnums<T extends object, R extends object>(base: T, derived: R): T & R;
